<can-component tag="app-page">
  <style type="less">
    display: block;
    
    p { font-weight: bold; }
  </style>
  <template>
    <p>{{message}}</p>
  </template>
  <view-model>
    import Map from 'can/map/';
    import 'can/map/define/';

    export default Map.extend({
      define: {
        message: {
          value: 'This is the app-page ONE component'
        }
      }
    });
  </view-model>
</can-component>